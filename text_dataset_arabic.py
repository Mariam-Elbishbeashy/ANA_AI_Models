#!/usr/bin/env python3
"""
HIGH-QUALITY EGYPTIAN ARABIC IFS Character Dataset Generator - FIXED ENCODING
Generates perfect Egyptian Arabic dataset with correct encoding and no garbled text
"""

import os
import sys
import csv
import json
import argparse
import random
import re
import time
import hashlib
import glob
from typing import List, Dict, Set, Tuple, Optional
from collections import defaultdict
from pathlib import Path
import unicodedata
import codecs

# ---------------- Configuration ----------------
SEED = 12345
random.seed(SEED)

# Define as a variable that can be modified
RAW_DATA_DIR = "data/raw"
OUT_DIR_DEFAULT = "data/processed"
TOTAL_EXAMPLES = 360000  # 20,000 per character × 18 characters
EXAMPLES_PER_CHARACTER = 20000

# ---------------- All Characters in Arabic ----------------
ALL_CHARACTERS = [
    "الناقد الداخلي", "الكمالي", "مرضي الناس", "المتحكم", "الجزء المتزن",
    "مدمن العمل", "الجزء المشوش", "المماطل", "الإفراط في الأكل", "مدمن الألعاب",
    "الجزء الوحيد", "الجزء الخائف", "الجزء المهمل", "الجزء المخزي", "الجزء المثقََّل",
    "الجزء المعتمد", "الجزء الغيور", "الطفل المجروح"
]

# Character mapping for consistency
CHARACTER_MAPPING = {
    "Inner Critic": "الناقد الداخلي",
    "Perfectionist": "الكمالي", 
    "People-Pleaser": "مرضي الناس",
    "Controller": "المتحكم",
    "Stoic Part": "الجزء المتزن",
    "Workaholic": "مدمن العمل",
    "Confused Part": "الجزء المشوش",
    "Procrastinator": "المماطل",
    "Overeater/Binger": "الإفراط في الأكل",
    "Excessive Gamer": "مدمن الألعاب",
    "Lonely Part": "الجزء الوحيد",
    "Fearful Part": "الجزء الخائف",
    "Neglected Part": "الجزء المهمل",
    "Ashamed Part": "الجزء المخزي",
    "Overwhelmed Part": "الجزء المثقََّل",
    "Dependent Part": "الجزء المعتمد",
    "Jealous Part": "الجزء الغيور",
    "Wounded Child": "الطفل المجروح"
}

# Emotion mapping in Arabic
EMOTION_MAPPING = {
    "sadness": "حزن",
    "fear": "خوف",
    "anger": "غضب",
    "neutral": "محايد"
}

# Category mapping in Arabic
CATEGORY_MAPPING = {
    "critical": "انتقادي",
    "anxious": "قلق",
    "protective": "وقائي", 
    "compulsive": "قَهْري",
    "avoidant": "تجنبي",
    "disoriented": "مشوش",
    "sad": "حزين",
    "shame": "خزي",
    "vulnerable": "هش"
}

# ---------------- Character Core Patterns in Proper Egyptian Arabic ----------------
CHARACTER_PATTERNS = {
    "الناقد الداخلي": {
        "emotion": "حزن",
        "category": "انتقادي",
        "core_phrases": [
            "مش كفاية كويس", "فاشل", "مش قيم", "خيبة أمل", "غير كافي",
            "مش كويس أبدًا", "دائمًا بفشل", "معرفش أعمل حاجة صح", "عبء",
            "مش مفيد", "مش فيه أمل", "غير كفء", "به عيوب", "بخرب على نفسي",
            "أسوأ عدو ليا", "مستحقش", "مش قيم", "فيه إيه فيَّ؟",
            "الغير أحسن مني", "فيه عيب جوهري", "بعمل فوضى في كل حاجة"
        ],
        "templates": [
            "بحس إني {}",
            "أنا دايماً {}",
            "مهما أعمل، بحس إني {}",
            "الصوت جوايا بيقولي إني {}",
            "مش عارف أبعد عن فكرة إنِّي {}",
            "من جوا، بآمن إني {}",
            "دايماً قلِق إني {}",
            "فكرة إنِّي {} مش بتبعد عني",
            "بعاني من إنِّي {}",
            "اتقالي إني {} من زمان فبقيت أصدق"
        ]
    },
    
    "الكمالي": {
        "emotion": "خوف",
        "category": "قلق",
        "core_phrases": [
            "لازم يكون مثالي", "مش فيه عيوب", "مفيش أخطاء", "ظبط تمام", "كل تفصيلة",
            "أعيد كل حاجة", "الكويس مش كفاية", "الموضوع إما مثالي أو ولا حاجة", "معايير عالية",
            "مش عارف أقبل أي عيب", "مهووس بالتفاصيل", "لازم يكون مش فيه أي عيب", "خايف من الأخطاء",
            "مفيش حاجة كويسة كفاية", "دائمًا براجع", "الدقة مهمة", "صفر تسامح في الأخطاء"
        ],
        "templates": [
            "كل حاجة {}",
            "أنا محتاج كل حاجة تكون {}",
            "مش هقدِّم شغل إلا لو هو {}",
            "فكرة {} بتخوفني",
            "بقعد ساعات عشان أتأكد إن الحاجات {}",
            "شغلي لازم يكون {} وإلا مش هشاركه",
            "دايماً قلِق على {}",
            "{} مش قابلة للمساومة بالنسبالي",
            "مش هعرف أرتاح إلا لما الحاجات {}",
            "{} بتحكِّم في كل عملية الشغل"
        ]
    },
    
    "مرضي الناس": {
        "emotion": "خوف",
        "category": "قلق",
        "core_phrases": [
            "بقول آه", "بتجنب المشاكل", "محتاج موافقة", "بِرضِي الناس", "احتياجات الناس",
            "عايز الكل يحبني", "مش عارف أقول لأ", "بخلي الناس تِقدَّم عليَّ", "مسؤول عن مشاعرهم",
            "خايف من الرفض", "بضحِّي باحتياجاتي", "بحافظ على السلام", "بغير نفسي عشان أناسب",
            "بتجنب الخلافات", "محتاج تأكيد", "بقدِّم الناس على نفسي",
            "خايف من الاستنكار", "بخدم الكل", "بنَسى نفسي في الناس"
        ],
        "templates": [
            "أنا دايماً {}",
            "مش عارف أوقف نفسي عن {}",
            "احتياجي إنِّي {} بيسيطر على حياتي",
            "أنا خايف من {}",
            "{} هو وضعي الافتراضي",
            "بحس إني مجبور إني {}",
            "أكبر خوف عندي هو {}",
            "بَنيَّت حياتي على {}",
            "{} بحسه زي البقاء بالنسبالي",
            "معرفش أوقف {}"
        ]
    },
    
    "المتحكم": {
        "emotion": "غضب",
        "category": "وقائي",
        "core_phrases": [
            "مسيطر", "طريقتي", "بتحكِّم في كل صغيرة وكبيرة", "محتاج نظام", "بشرف على كل حاجة",
            "فقدان السيطرة بيخوفني", "الفوضى بتكون نتيجة", "مش عارف أوكِّل", "محتاج أدير",
            "كل حاجة لازم تِتْبَع خطتي", "العشوائية متعباني", "عدم القدرة على التوقع خطر",
            "لازم أشرف", "الحاجات بتنهار من غيري", "مشكلات ثقة", "محتاج ترتيب",
            "النظام ده أحسن حاجة", "قلِق لما مش أنا المسيطر", "بفضل الحاجات المتوقعة"
        ],
        "templates": [
            "أنا محتاج أكون {}",
            "{} أساسي بالنسبالي",
            "لما مش {}, ببقى مذعور",
            "احتياجي إني {} بيأثِّر في علاقاتي",
            "{} بحسه زي الأمان بالنسبالي",
            "مش هعرف أشتغل من غير {}",
            "{} ده طريقتي في التعامل مع عدم التأكد",
            "إصراري على {} بيسبب مشاكل",
            "بحس بالأمان لما {}",
            "{} بقى آلية تعاملي"
        ]
    },
    
    "الجزء المتزن": {
        "emotion": "محايد",
        "category": "وقائي",
        "core_phrases": [
            "مش بظهر مشاعري", "تحكم في المشاعر", "منفصل", "هادئ", "مش عاطفي",
            "بخلي المشاعر جوايا", "المشاعر ضعف", "بفضل الهدوء", "بكبت المشاعر",
            "عقلاني مش عاطفي", "المشاعر بتعطِّل", "بحافظ على رباطة جأشي", "مسافة عاطفية",
            "مش بعبِّر عن مشاعري", "بفضل الوجه الهادئ", "حوائط عاطفية", "بتجنب الهشاشة"
        ],
        "templates": [
            "أنا نادراً ما {}",
            "{} هو وضعي الطبيعي",
            "الناس بتقول عليّ إنِّي {} زيادة",
            "اتعلمت إني {}",
            "{} بيحميني من الأذى",
            "اتجاهي إنِّي {} بيعزلني",
            "بحس بالأمان لما أنا {}",
            "{} بقى آلية دفاعي",
            "معرفش أوقف {}",
            "{} بحسه زي الخيار الوحيد"
        ]
    },
    
    "مدمن العمل": {
        "emotion": "محايد",
        "category": "قَهْري",
        "core_phrases": [
            "دايماً بشتغل", "مشغول أوي", "منتج", "مش عارف أوقف الشغل", "هويتي في الشغل",
            "قيمتي متَّصلة بالشغل", "بشتغل زيادة", "مُتَّعرف بالشغل", "الراحة بتخليني أحس بالذنب",
            "مركِّز على الإنجاز", "إنتاجية دائمة", "الشغل بيكون أولوية", "بتنجح في الشغل",
            "خايف من قلة الإنتاجية", "بستخدم الشغل في التهرب", "اتجاهات إدمان الشغل", "بهمل نفسي"
        ],
        "templates": [
            "أنا دايماً {}",
            "{} بيُعرِّف مين أنا",
            "احتياجي إني {} بيستهلكني",
            "مش متخيل حياتي من غير {}",
            "{} هو طريقتي في إثبات قيمتي",
            "بستخدم {} عشان أتهرب من مشاكل تانية",
            "هويتي متَّصلة ب{}",
            "{} بحسه زي هدفي",
            "بحس نفسي ضايع لما مش {}",
            "{} سيطر على حياتي"
        ]
    },
    
    "الجزء المشوش": {
        "emotion": "خوف",
        "category": "مشوش",
        "core_phrases": [
            "مش فاهم", "مش عارف", "مختلط عليَّ", "مش واضح", "متردد",
            "دماغي ضبابية", "مش عارف أفكر كويس", "كل حاجة مش واضحة", "ضائع",
            "مش متأكد", "مش عارف اتعامل", "ضباب في المخ", "مش فيه وضوح", "متحيِّر",
            "محير", "ممزق بين اختيارات", "مش عارف أفهم", "مش عارف أقرر مع كتر الخيارات"
        ],
        "templates": [
            "بحس دايماً إني {}",
            "{} هو وضعي الافتراضي",
            "دماغي دايماً {}",
            "بعاني من إنِّي {}",
            "{} بيعقِّد قراراتي اليومية",
            "مش عارف أهرب من حس إنِّي {}",
            "{} بيأثِّر على كل حاجة بعملها",
            "إنِّي {} دايماً بيوقفني",
            "يا ريت أقدر أوقف حس إنِّي {}",
            "{} بيخلي رؤيتي مش واضحة"
        ]
    },
    
    "المماطل": {
        "emotion": "محايد",
        "category": "تجنبي",
        "core_phrases": [
            "هعمله بعدين", "بكرا", "بأجِّل", "بتأخر", "آخر لحظة",
            "بتجنب ما أبدء", "مش في المود", "مستنى الحماس", "بشتغل تحت الضغط",
            "فيه وقت كتير", "كمان واحد بس", "مستعد له", "بتجنب المهام", "بتأخر في العمل",
            "عادة المماطلة", "مستنى اللحظة المناسبة", "أنا في المستقبل هتعامل مع الموضوع"
        ],
        "templates": [
            "أنا دايماً {}",
            "عِدَّتي إني {} بتيجيب مشاكل",
            "بقول لنفسي إنِّي ه{}",
            "{} هو آلية تعاملي",
            "مش عارف أوقف {}",
            "اتجاهي إني {} بيأثِّر على حياتي",
            "{} بحسه أسهل في اللحظة",
            "عارف إنِّي لازم أوقف {}",
            "{} بقى نمط",
            "بستخدم {} عشان أتهرب من عدم الراحة"
        ]
    },
    
    "الإفراط في الأكل": {
        "emotion": "حزن",
        "category": "قَهْري",
        "core_phrases": [
            "مش عارف أوقف الأكل", "الأكل العاطفي", "بنهم", "إدمان الأكل", "بآكل لما أكون متوتر",
            "بستخدم الأكل في التعامل", "الأكل بيريحني", "بآكل عشان أخدِّر نفسي", "بآكل في السر",
            "بحس بالذنب بعد الأكل", "خزي من الأكل", "مش مسيطر على الأكل", "بسد مشاعري بالأكل",
            "بتخدير نفسي بالأكل", "الأكل هو هروبي", "الأكل القَهْري", "بآكل لما يكون عندي مشاعر",
            "مشاكل في علاقتي مع الأكل"
        ],
        "templates": [
            "لما أكون متوتر، أنا {}",
            "بستخدم الأكل عشان {}",
            "نمطي في {} بيخليني أحس بالخزي",
            "{} هو طريقتي في التعامل مع المشاعر",
            "بحس إني مش مسيطر لما {}",
            "{} بيقدِّم راحة مؤقتة",
            "علاقتي مع الأكل فيها {}",
            "بعمل على إني أوقف {}",
            "{} بحسه زي الراحة الوحيدة",
            "بعاني من {} دايماً"
        ]
    },
    
    "مدمن الألعاب": {
        "emotion": "حزن",
        "category": "قَهْري",
        "core_phrases": [
            "بلعب طول الليل", "بتهرب من خلال الألعاب", "العالم الافتراضي", "إدمان الألعاب", "مهووس",
            "بتجنب العالم الحقيقي", "الهروب الرقمي", "بفضل الحياة الافتراضية", "مكافآت الألعاب", "هوية اللاعب",
            "تجنب الواقع", "بستخدم الألعاب في التعامل", "مش عارف أوقف اللعب", "الألعاب أهم من المسؤوليات",
            "نجاح افتراضي", "مجتمع الألعاب", "برفع المستوى في كل حاجة"
        ],
        "templates": [
            "بقعد ساعات {}",
            "{} بيساعدني أهرب من الواقع",
            "عِدَّتي إني {} بتقلقني",
            "بستخدم الألعاب عشان {}",
            "{} بحسه أحسن من الحياة الحقيقية",
            "اتجاهي إني {} بيأثِّر على مسؤولياتي",
            "{} بيقدِّم اللي الحياة الحقيقية مش بتقدِّمه",
            "قلِق على {}",
            "{} بقى طريقي الأساسي في التعامل",
            "بحس إني عايش لما {}"
        ]
    },
    
    "الجزء الوحيد": {
        "emotion": "حزن",
        "category": "حزين",
        "core_phrases": [
            "وحيد", "لوحدي", "منعزل", "منقطع", "مفيش حد فاهم",
            "مش عايزينني", "مهمل", "مش معاهم", "فاضِي جوايا", "بحن للاتصال",
            "بحس إني منفصل", "عزلة عاطفية", "بحن للرفقة", "مش محبوب",
            "برَّا المجموعة", "ناقصلي اتصال", "فراغ جوايا", "منعزل اجتماعيًا"
        ],
        "templates": [
            "بحس إني {} قوي",
            "حس {} ده مش بيروح",
            "إنِّي {} دايمًا بيأثِّر على سعادتي",
            "بعاني من إنِّي {}",
            "{} بيصبغ كل تجاربي",
            "حس إنِّي {} ده كبير أوي",
            "مش عارف أهرب من {}",
            "حياتي متَّعرفة ب{}",
            "{} بيعقِّد المواقف الاجتماعية",
            "يا ريت أقدر أوقف إنِّي {} قوي"
        ]
    },
    
    "الجزء الخائف": {
        "emotion": "خوف",
        "category": "قلق",
        "core_phrases": [
            "خايف", "مذعور", "قلِق", "متوتر", "هلع",
            "أسوأ سيناريو", "خطر", "مش آمن", "هش", "خوف من الفشل",
            "قلق متوقع", "مُرعوب", "إيه لو حصل حاجة وحشة", "متوجس",
            "قلق دائم", "تفكير كارثي", "متجنب للمخاطرة", "عقلية خايفة"
        ],
        "templates": [
            "أنا دايماً {}",
            "دماغي دايماً بتروح ل{}",
            "{} بيسيطر على قراراتي",
            "بعيش في حالة {}",
            "اتجاهي إني {} بيحد مني",
            "{} بحسه زي رفيقي الدائم",
            "مش عارف أهرب من حس إنِّي {}",
            "حياتي متحكَّمة ب{}",
            "{} بيوقفني عن المجازفة",
            "يا ريت أقدر أوقف إنِّي {}"
        ]
    },
    
    "الجزء المهمل": {
        "emotion": "حزن",
        "category": "حزين",
        "core_phrases": [
            "مهمل", "متجاهَل", "مش ملحوظ", "مش واخد باله مني", "مش مهم",
            "مش مرئي", "مش متشاف", "مش متسمع", "بيعمل عشان يتلفت انتباه", "إهمال عاطفي",
            "احتياجات مش متحققة", "مش متقدَّم", "منسِي", "مش متابع", "بحن للانتباه",
            "بحس إني مش مهم", "شخص في الخلفية", "مش الأولوية أبدًا"
        ],
        "templates": [
            "دايماً بحس إني {}",
            "حس إنِّي {} ده بيوجع",
            "تجربتي مع {} بتأثِّر على قيمتي الذاتية",
            "بعاني من إنِّي {}",
            "{} كان نمط في حياتي",
            "حس إنِّي {} ده مؤلم",
            "مش عارف أهز حس إنِّي {}",
            "{} بيصعب عليَّ أطلب اللي أنا محتاجه",
            "طفولتي كانت فيها {} كتير",
            "بعمل على مشاعر {}"
        ]
    },
    
    "الجزء المخزي": {
        "emotion": "حزن",
        "category": "خزي",
        "core_phrases": [
            "مخزي", "خزي", "محرج", "متذنِّب", "به عيوب",
            "شخص وحش", "فشل أخلاقي", "سر وسخ", "مُهان", "ندمان",
            "نادم", "واعي لنفسي", "محكوم عليَّ", "معيب", "مستحقش",
            "ملوَّث", "خزي مخفي", "خزي جوهري"
        ],
        "templates": [
            "أنا بحمل {} عميق",
            "حس {} ده مش بيروح",
            "حسي ب{} بيأثِّر على كل حاجة",
            "بعاني من {}",
            "{} بيصبغ إزاي بشوف نفسي",
            "وزن {} ده ثقيل",
            "مش عارف أهرب من {}",
            "{} بيصعب عليَّ الاتصال",
            "حياتي متَّسمة ب{}",
            "بعمل على أتشافى من {}"
        ]
    },
    
    "الجزء المثقََّل": {
        "emotion": "خوف",
        "category": "قلق",
        "core_phrases": [
            "مثقََّل", "كتير أوي", "مش قادر أتحمل", "غرقان", "ضغط",
            "توتر", "احتراق نفسي", "متعَب", "حِمل حسي زائد", "مطالب متعددة",
            "مغْرَق", "مش غاطس", "مدفون", "محطوط عليه", "حِمل معلومات زائد",
            "حِمل عاطفي زائد", "مشدود في اتجاهات", "مفيش مساحة", "محتاج راحة"
        ],
        "templates": [
            "بحس دايماً إني {}",
            "حالة {} دي مرهقة",
            "اتجاهي إني {} بيأثِّر على صحتي",
            "بعاني من إنِّي {}",
            "{} بيصعب المهام اليومية",
            "حس إنِّي {} مش بيفضى",
            "مش عارف أوقف إنِّي {}",
            "{} بيوقفني عن إنِّي أشتغل كويس",
            "حياتي متَّعرفة ب{}",
            "بعمل على إدارة {} الدائم"
        ]
    },
    
    "الجزء المعتمد": {
        "emotion": "خوف",
        "category": "قلق",
        "core_phrases": [
            "مش عارف أعمل لوحدي", "محتاج مساعدة", "معتمد", "بِعتمَد على الناس", "مش عارف أتصرَّف",
            "معتمد على الآخرين", "محتاج ناس", "خايف من الاستقلالية", "متعلِّق", "محتاج إرشاد",
            "مش قادر لوحدي", "متشابك", "مختلط", "مفيش استقلالية", "مشكلات ارتباط",
            "محتاج تأكيد", "بدور على موافقة", "بدور على الناس", "بِتْبَع الناس"
        ],
        "templates": [
            "بعاني من {}",
            "احتياجي إني {} بيقلقني",
            "بحس إني {} في العلاقات",
            "نمطي في {} بيوقفني",
            "{} بيأثِّر على استقلاليتي",
            "مش عارف أوقف {}",
            "اتجاهي إني {} بيقلقني",
            "{} بيصعب عليَّ أصدق نفسي",
            "بعمل على إنِّي أتغلب على {}",
            "{} كان نمط في حياتي كلها"
        ]
    },
    
    "الجزء الغيور": {
        "emotion": "غضب",
        "category": "وقائي",
        "core_phrases": [
            "غيور", "حاسد", "مستاء", "مقارنة", "الغير عندهم اللي أنا عايزه",
            "ليه هم مش أنا", "تملكي", "خايف أخسر", "غير كافي بالمقارنة", "الحياة مش عادلة",
            "بحسد الناس على اللي عندهم", "عينه خضرا", "إقليمي", "مش متأكد", "مش واثق",
            "منافس", "تنافس", "يا ريت عندي", "مر من نجاح الناس"
        ],
        "templates": [
            "بحس {} لما الناس تنجح",
            "اتجاهي إني {} بيخجلني",
            "بعاني من مشاعر {}",
            "{} بيأثِّر على علاقاتي",
            "مش عارف أوقف حس إنِّي {}",
            "نمطي في {} بيقلقني",
            "{} بيصعب عليَّ أكون فرحان للناس",
            "بعمل على {}",
            "{} بحسه زي رد الفعل التلقائي",
            "مشاعر {} بتقلقني"
        ]
    },
    
    "الطفل المجروح": {
        "emotion": "حزن",
        "category": "هش",
        "core_phrases": [
            "الطفل الداخلي", "جرح الطفولة", "أذى من الماضي", "جرح عاطفي", "صدمة",
            "طفل هش", "طفل متروك", "طفل مرفوض", "طفل متأذي", "جرح جوهري",
            "نفسي الصغيرة", "طفل محتاج", "طفل خايف", "طفل وحيد", "طفل مخزي",
            "جرح قديم", "صدمة تطورية", "جرح ارتباط", "ألم مش متشافي"
        ],
        "templates": [
            "{} بتأثِّر عليَّ لسه",
            "أنا بحمل {} معايا",
            "{} من ماضيّي بتأثِّر على الحاضر",
            "بعمل على أتشافى من {}",
            "{} محتاجة انتباه",
            "{} بتشكِّل إزاي بتربط",
            "بتعلم إني أربِّي {}",
            "وجع {} بيطلع كثير",
            "{} بحسها حساسة أحيانًا",
            "بعمل مع {}"
        ]
    }
}

# ---------------- Arabic Language Enhancement ----------------
ARABIC_PREFIXES = [
    "بصراحة، ", "أحياناً ", "مؤخراً، ", "كثيراً ", "في الفترة الأخيرة، ",
    "الحقيقة إنّ ", "لاحظت إني ", "بحس إني ", "بيبدو إني ",
    "في تجربتي، ", "بالنسبالي، ", "شخصياً، ", "بصراحة شديدة، ",
    "لازم أعترف إني ", "مستوعب إني ", "بتعلم إني ",
    "أكتر وأكتر، ", "في زيادة، ", "هذه الأيام، ", "دلوقتي، "
]

ARABIC_SUFFIXES = [
    ".", " وصعب أوي.", " وعامل على الموضوع.",
    " وبيأثِّر على حياتي اليومية.", " ومش عارف أعمل إيه فيه.",
    " ومرهق أوي.", " بس بعمل على أتغير.",
    " وبيأثِّر على علاقاتي.", " مع إنِّي مش عايز يكون كده.",
    " ومش عارف أصلحه.", " وبيخلي كل حاجة أصعب.",
    " وبيأثِّر على صحتي.", " بس التغيير شبه مستحيل.",
    " وتعبان من الإحساس ده.", " وده اللي خلاني أدور على مساعدة.",
    " وده بقى نمط.", " بس الوعي هو أول خطوة.",
    " وبتعلم أتقبله.", " وبيحتاج اهتمام دائم.",
    " وبتطوِّر استراتيجيات أتعامل بيها."
]

ARABIC_CONTEXTS = [
    "لما أكون في الشغل، ", "في العلاقات، ", "مع عيلتي، ",
    "حوالين صحابي، ", "لما أكون لوحدي، ", "في الأوقات الصعبة، ",
    "لما أكون منزعج، ", "في المواقف الاجتماعية، ", "لما أقابل تحديات، ",
    "في حياتي اليومية، ", "لما بأخد قرارات، ", "في مواقف جديدة، ",
    "لما الحاجات بتكون صعبة، ", "في الخلافات، ", "لما أكون تعبان، ",
    "لما التوقعات عالية، ", "في أماكن مش معتاد عليها، ", "لما لازم أنجح، ",
    "لما الناس تِتْعَمَد عليَّ، ", "لما أكون تحت ضغط، "
]

ARABIC_INTENSIFIERS = [
    "عميقاً", "دايماً", "أبداً", "مش أبداً", "دايماً",
    "كبير أوي", "عميق جداً", "قوي", "باستمرار",
    "مزمن", "عادةً", "تلقائياً", "انعكاسياً",
    "من غير وعي", "غريزياً", "مش ممكن الهروب منه", "في كل حتة"
]

# ---------------- Arabic Text Processing ----------------
def normalize_arabic_text(text: str) -> str:
    """Normalize Arabic text for consistency."""
    if not text:
        return ""
    
    # Normalize Unicode to composed form
    text = unicodedata.normalize('NFC', text)
    
    # Fix common Arabic spacing issues
    text = re.sub(r'[ـ_]', '', text)  # Remove tatweel/kashida
    text = re.sub(r'\s+', ' ', text)  # Normalize whitespace
    text = text.strip()
    
    # Ensure proper Arabic punctuation
    text = re.sub(r'([\u0600-\u06FF])\.', r'\1.', text)
    
    return text

def validate_arabic_text(text: str) -> bool:
    """Validate that text contains proper Arabic characters and structure."""
    if not text:
        return False
    
    # Check for minimum Arabic content (at least 30% of characters should be Arabic)
    arabic_chars = sum(1 for c in text if '\u0600' <= c <= '\u06FF' or c in ' .،؛؟')
    if arabic_chars < len(text) * 0.3:
        return False
    
    # Check for proper sentence ending
    if not text.endswith(('.', '!', '؟', '،')):
        return False
    
    # Check for reasonable length
    if len(text) < 10 or len(text) > 500:
        return False
    
    return True

def ensure_arabic_encoding(text: str) -> str:
    """Ensure text is properly encoded in UTF-8 and contains valid Arabic."""
    try:
        # Try to decode as UTF-8 first
        text.encode('utf-8').decode('utf-8')
    except:
        # If not UTF-8, try to fix it
        try:
            text = text.encode('latin-1').decode('utf-8')
        except:
            text = text.encode('utf-8', 'ignore').decode('utf-8', 'ignore')
    
    return text

# ---------------- Helper Functions ----------------
def clean_text(text: str) -> str:
    """Clean text while preserving meaning."""
    if not text:
        return ""
    
    text = ensure_arabic_encoding(text)
    text = normalize_arabic_text(text)
    
    # Remove extra whitespace
    text = re.sub(r'\s+', ' ', text)
    
    # Ensure proper sentence structure
    if not text.endswith(('.', '!', '؟', '،')):
        text = text + '.'
    
    return text

def load_raw_datasets(raw_data_dir: str) -> Dict[str, List[str]]:
    """
    Load all text datasets from the raw data directory.
    Returns a dictionary mapping dataset names to lists of sentences.
    """
    raw_data = {}
    
    if not os.path.exists(raw_data_dir):
        print(f"⚠️ تحذير: مجلد البيانات الخام '{raw_data_dir}' مش موجود.")
        print(f"   هنعمل بيانات اصطناعية بدل كده.")
        return raw_data
    
    # Find all text files
    text_files = []
    for ext in ['*.txt', '*.csv', '*.json', '*.jsonl']:
        text_files.extend(glob.glob(os.path.join(raw_data_dir, '**', ext), recursive=True))
        text_files.extend(glob.glob(os.path.join(raw_data_dir, ext)))
    
    print(f"📂 لقينا {len(text_files)} ملف بيانات خام")
    
    for file_path in text_files:
        filename = os.path.basename(file_path)
        try:
            if filename.endswith('.txt'):
                with open(file_path, 'r', encoding='utf-8', errors='ignore') as f:
                    content = f.read()
                    # Split into sentences (Arabic punctuation)
                    sentences = re.split(r'[.!؟]+', content)
                    sentences = [clean_text(s) for s in sentences if clean_text(s)]
                    if sentences:
                        raw_data[filename] = sentences
                        print(f"   حَمَّلنا {len(sentences)} جملة من {filename}")
            
            elif filename.endswith('.csv'):
                with open(file_path, 'r', encoding='utf-8', errors='ignore') as f:
                    reader = csv.reader(f)
                    sentences = []
                    for row in reader:
                        if row:
                            text = row[0] if len(row) > 0 else ' '.join(row)
                            if text and len(text.strip()) > 10:
                                sentences.append(clean_text(text))
                    if sentences:
                        raw_data[filename] = sentences
                        print(f"   حَمَّلنا {len(sentences)} صف من {filename}")
            
            elif filename.endswith('.json') or filename.endswith('.jsonl'):
                sentences = []
                with open(file_path, 'r', encoding='utf-8', errors='ignore') as f:
                    if filename.endswith('.jsonl'):
                        for line in f:
                            try:
                                data = json.loads(line.strip())
                                if isinstance(data, dict) and 'text' in data:
                                    sentences.append(clean_text(data['text']))
                                elif isinstance(data, str):
                                    sentences.append(clean_text(data))
                            except:
                                pass
                    else:
                        try:
                            data = json.load(f)
                            if isinstance(data, list):
                                for item in data:
                                    if isinstance(item, dict) and 'text' in item:
                                        sentences.append(clean_text(item['text']))
                                    elif isinstance(item, str):
                                        sentences.append(clean_text(item))
                            elif isinstance(data, dict):
                                for key, value in data.items():
                                    if isinstance(value, str) and len(value) > 10:
                                        sentences.append(clean_text(value))
                        except:
                            pass
                
                if sentences:
                    raw_data[filename] = sentences
                    print(f"   حَمَّلنا {len(sentences)} عنصر من {filename}")
        
        except Exception as e:
            print(f"   غلط في تحميل {filename}: {str(e)}")
    
    # Combine all sentences into one large list
    all_sentences = []
    for sentences in raw_data.values():
        all_sentences.extend(sentences)
    
    print(f"\n📊 إجمالي الجمل المحملة: {len(all_sentences)}")
    return {"all_sentences": all_sentences, **raw_data}

def extract_patterns_from_text(text: str, character: str) -> List[str]:
    """
    Extract relevant patterns and phrases from raw text for a specific character.
    """
    patterns = []
    
    # Character-specific Arabic keywords
    character_keywords = {
        "الناقد الداخلي": ["فشل", "قيم", "كفاية", "خيبة", "عبء"],
        "الكمالي": ["مثالي", "عيوب", "خطأ", "تفصيلة", "معيار", "يراجع"],
        "مرضي الناس": ["يرضي", "موافقة", "مشكلة", "آه", "رفض", "غير"],
        "المتحكم": ["سيطرة", "يتحكم", "نظام", "خطة", "ترتيب", "فوضى"],
        "الجزء المتزن": ["مشاعر", "منفصل", "هادئ", "يكبت", "عقلاني", "عاطفي"],
        "مدمن العمل": ["شغل", "مشغول", "منتج", "إنجاز", "يشتغل", "راحة"],
        "الجزء المشوش": ["مش فاهم", "غير واضح", "متردد", "ضبابية", "غير متأكد", "ضائع"],
        "المماطل": ["بعدين", "بكرا", "يؤجل", "يأخر", "لحظة", "يتجنب"],
        "الإفراط في الأكل": ["يأكل", "أكل", "بنهم", "عاطفي", "يخدر", "راحة"],
        "مدمن الألعاب": ["يلعب", "ألعاب", "افتراضي", "يهرب", "مدمن", "مهووس"],
        "الجزء الوحيد": ["وحيد", "لوحدي", "منعزل", "منقطع", "غير مرغوب", "فاضِي"],
        "الجزء الخائف": ["خايف", "مذعور", "قلِق", "متوتر", "هلع", "خوف"],
        "الجزء المهمل": ["مهمل", "متجاهَل", "غير مرئي", "غير مسموع", "غير مرئي", "مش متشاف"],
        "الجزء المخزي": ["مخزي", "خزي", "محرج", "متذنِّب", "غير قيم", "معيب"],
        "الجزء المثقََّل": ["مثقََّل", "كثير", "يتحمل", "غرقان", "ضغط", "توتر"],
        "الجزء المعتمد": ["يعتمد", "مساعدة", "معتمد", "يتعلق", "إرشاد", "مستقل"],
        "الجزء الغيور": ["غيور", "حاسد", "مستاء", "مقارنة", "تملكي", "منافس"],
        "الطفل المجروح": ["طفل", "جرح", "صدمة", "متأذي", "متروك", "مرفوض"]
    }
    
    keywords = character_keywords.get(character, [])
    text = ensure_arabic_encoding(text)
    
    # Find sentences containing keywords
    for keyword in keywords:
        if keyword in text:
            # Extract phrase around keyword
            words = text.split()
            for i, word in enumerate(words):
                if keyword in word:
                    # Get context window around keyword
                    start = max(0, i - 3)
                    end = min(len(words), i + 4)
                    phrase = ' '.join(words[start:end])
                    patterns.append(phrase)
                    break
    
    return patterns

def enhance_character_patterns(raw_data: Dict[str, List[str]]) -> Dict:
    """
    Enhance character patterns with patterns extracted from raw data.
    """
    enhanced_patterns = CHARACTER_PATTERNS.copy()
    
    all_sentences = raw_data.get("all_sentences", [])
    if not all_sentences:
        print("⚠️ مفيش جمل خام، هنستخدم الأنماط الأساسية بس")
        return enhanced_patterns
    
    print("\n🔍 بنستخرج أنماط من البيانات الخام...")
    
    for character in ALL_CHARACTERS:
        character_patterns = []
        
        # Extract patterns from raw sentences
        for sentence in all_sentences:
            patterns = extract_patterns_from_text(sentence, character)
            character_patterns.extend(patterns)
        
        # Remove duplicates and clean
        character_patterns = list(set(character_patterns))
        character_patterns = [clean_text(p) for p in character_patterns if len(clean_text(p)) > 10]
        
        if character_patterns:
            # Add to core phrases (limit to 10 additional patterns)
            additional_patterns = character_patterns[:10]
            enhanced_patterns[character]["core_phrases"].extend(additional_patterns)
            
            # Ensure unique phrases
            enhanced_patterns[character]["core_phrases"] = list(set(enhanced_patterns[character]["core_phrases"]))
            
            print(f"   {character}: ضفنا {len(additional_patterns)} نمط")
    
    return enhanced_patterns

def create_realistic_arabic_variation(base_template: str, core_phrase: str, 
                                     raw_sentences: List[str], variation_num: int) -> str:
    """
    Create a realistic Arabic variation using raw sentence patterns.
    """
    rng = random.Random(SEED + hash(base_template) + hash(core_phrase) + variation_num)
    
    # 30% chance to use raw sentence structure
    if raw_sentences and rng.random() < 0.3:
        raw_sentence = rng.choice(raw_sentences)
        if core_phrase in raw_sentence:
            text = raw_sentence
        else:
            words = raw_sentence.split()
            if len(words) > 3:
                insert_pos = rng.randint(1, len(words) - 2)
                words.insert(insert_pos, core_phrase)
                text = ' '.join(words)
            else:
                text = base_template.format(core_phrase)
    else:
        text = base_template.format(core_phrase)
    
    # Apply random Arabic modifications
    modifications = []
    
    # Add Arabic prefix (30% chance)
    if rng.random() < 0.3:
        prefix = rng.choice(ARABIC_PREFIXES)
        modifications.append(("prefix", prefix))
    
    # Add Arabic suffix (40% chance)
    if rng.random() < 0.4:
        suffix = rng.choice(ARABIC_SUFFIXES)
        modifications.append(("suffix", suffix))
    
    # Add Arabic context (25% chance)
    if rng.random() < 0.25:
        context = rng.choice(ARABIC_CONTEXTS)
        modifications.append(("context", context))
    
    # Apply modifications
    for mod_type, mod_text in modifications:
        if mod_type == "prefix":
            text = mod_text + text
        elif mod_type == "suffix":
            text = text.rstrip('.!؟') + mod_text
        elif mod_type == "context":
            text = mod_text + text
    
    return clean_text(text)

def create_compound_arabic_variation(char1_data: dict, char2_data: dict, 
                                    raw_sentences: List[str], variation_num: int) -> str:
    """Create a realistic compound Arabic variation combining two character patterns."""
    rng = random.Random(SEED + variation_num * 1000)
    
    # 20% chance to use raw sentence for compound
    if raw_sentences and rng.random() < 0.2:
        raw_sentence = rng.choice(raw_sentences)
        char1_phrases = [p for p in char1_data["core_phrases"] if p in raw_sentence]
        char2_phrases = [p for p in char2_data["core_phrases"] if p in raw_sentence]
        
        if char1_phrases and char2_phrases:
            return clean_text(raw_sentence)
    
    # Get random elements from each character
    phrase1 = rng.choice(char1_data["core_phrases"])
    phrase2 = rng.choice(char2_data["core_phrases"])
    
    # Create Arabic compound patterns
    patterns = [
        f"بلاحظ إني لما {phrase1}، فأنا كمان يميل إني {phrase2}",
        f"بطريقة ما {phrase1} و{phrase2} مرتبطين عندي",
        f"بيبدو إني {phrase1} و{phrase2} مرتبطين في حالتي",
        f"بدأت أشوف إزاي {phrase1} متعلق بـ{phrase2}",
        f"لما بحس إني {phrase1}، ده بيجي بـ{phrase2}",
        f"المعالج النفسي أشار لعلاقة بين {phrase1} و{phrase2}",
        f"لاحظت نمط: لما أكون {phrase1}، فأنا أكتر عرضة إنِّي {phrase2}",
        f"الاتصال بين {phrase1} و{phrase2} بقى أوضح بالنسبالي",
        f"صعب أفصل {phrase1} عن {phrase2}",
        f"فيه أيام، {phrase1} و{phrase2} بيكونوا متشابكين"
    ]
    
    text = rng.choice(patterns)
    
    # Apply random Arabic modifications
    if rng.random() < 0.3:
        text = rng.choice(ARABIC_PREFIXES) + text
    if rng.random() < 0.4:
        text = text.rstrip('.!؟') + rng.choice(ARABIC_SUFFIXES)
    
    return clean_text(text)

def generate_character_cues(character: str) -> str:
    """Generate character cues string in Arabic."""
    if character in CHARACTER_PATTERNS:
        phrases = CHARACTER_PATTERNS[character]["core_phrases"][:10]
        return "؛ ".join(phrases)
    return character

def create_arabic_advanced_variation(character: str, core_phrases: List[str], 
                                    variation_num: int) -> str:
    """Create advanced Arabic variations with complex structures."""
    rng = random.Random(SEED + variation_num * 2000)
    
    phrase1 = rng.choice(core_phrases)
    phrase2 = rng.choice(core_phrases)
    
    # Advanced Arabic patterns
    advanced_patterns = [
        f"اللي بافهمه دلوقتي إنِّي {phrase1} وده الوعي ده بيغير طريقة تعاملي مع الحاجات",
        f"رحلتي مع {phrase1} علمتني دروس مهمة عن نفسي",
        f"العمل على {phrase1} كان تحدي بس تحوَّل لحاجة إيجابية",
        f"بدأت أفهم جذور {phrase1}",
        f"نمط {phrase1} بيظهر أقوى لما {phrase2}",
        f"المعالج النفسي بيقول إنِّي {phrase1} جايبها من {phrase2}",
        f"بلاحظ إني {phrase1} بيحصل لما بحس إني {phrase2}",
        f"تعلمت إدارة {phrase1} كان جزء أساسي من نموي",
        f"كل ما بَشفي من {phrase1}، كل ما بختبر {phrase2} أقل",
        f"{phrase1} كانت مسيطرة على حياتي، لكن دلوقتي بتعلم إني {phrase2}"
    ]
    
    text = rng.choice(advanced_patterns)
    
    # Add Arabic modifications
    if rng.random() < 0.3:
        text = rng.choice(ARABIC_PREFIXES) + text
    if rng.random() < 0.4:
        text = text.rstrip('.!؟') + rng.choice(ARABIC_SUFFIXES)
    
    return clean_text(text)

# ---------------- Main Generation Function ----------------
def generate_high_quality_arabic_dataset(raw_data_dir: str, out_dir: str, examples_per_char: int = EXAMPLES_PER_CHARACTER):
    """Generate high-quality Arabic dataset with perfect language and no duplicates."""
    os.makedirs(out_dir, exist_ok=True)
    output_path = os.path.join(out_dir, "ifs_arabic_dataset.csv")
    
    print("=" * 70)
    print("🎯 بِنْشِئ مجموعة بيانات IFS بالعربي عالية الجودة")
    print("=" * 70)
    
    # Load raw data
    print("📂 بنحمِّل البيانات الخام...")
    raw_data = load_raw_datasets(raw_data_dir)
    
    # Enhance character patterns with raw data
    print("\n🔧 بنحسِّن أنماط الشخصيات بالبيانات الخام...")
    enhanced_patterns = enhance_character_patterns(raw_data)
    
    all_sentences = raw_data.get("all_sentences", [])
    
    print(f"\n📊 تكوين مجموعة البيانات:")
    print(f"   عدد الشخصيات: {len(ALL_CHARACTERS)}")
    print(f"   أمثلة لكل شخصية: {examples_per_char:,}")
    print(f"   إجمالي الأمثلة: {examples_per_char * len(ALL_CHARACTERS):,}")
    print(f"   الجمل الخام المتاحة: {len(all_sentences)}")
    print("=" * 70)
    
    start_time = time.time()
    
    # Use codecs to ensure UTF-8 encoding with BOM for Excel compatibility
    with codecs.open(output_path, "w", encoding="utf-8-sig") as f:
        writer = csv.writer(f)
        writer.writerow(["id", "input_text", "detected_emotion", "inner_char", "character_cues", "category"])
        
        row_id = 1
        total_generated = 0
        global_text_set = set()
        
        for character_idx, character in enumerate(ALL_CHARACTERS):
            char_start_time = time.time()
            print(f"\n📝 بنشتغل على: {character}")
            
            char_data = enhanced_patterns[character]
            emotion = char_data["emotion"]
            category = char_data["category"]
            cues = generate_character_cues(character)
            
            templates = char_data["templates"]
            core_phrases = char_data["core_phrases"]
            
            char_text_set = set()
            examples_generated = 0
            
            # Phase 1: Basic template variations (60% of examples)
            print(f"  المرحلة 1: تنويعات أساسية...")
            basic_target = int(examples_per_char * 0.6)
            
            template_idx = 0
            phrase_idx = 0
            variation_num = 0
            
            while examples_generated < basic_target:
                template = templates[template_idx % len(templates)]
                phrase = core_phrases[phrase_idx % len(core_phrases)]
                
                text = create_realistic_arabic_variation(template, phrase, all_sentences, variation_num)
                text_hash = hashlib.md5(text.encode('utf-8')).hexdigest()
                
                # Validate Arabic text before adding
                if validate_arabic_text(text) and text_hash not in global_text_set and text_hash not in char_text_set:
                    global_text_set.add(text_hash)
                    char_text_set.add(text_hash)
                    
                    writer.writerow([row_id, text, emotion, character, cues, category])
                    row_id += 1
                    examples_generated += 1
                    total_generated += 1
                    
                    if examples_generated % 1000 == 0:
                        print(f"    ولَّدنا {examples_generated:,}/{basic_target:,} مثال أساسي")
                        f.flush()
                
                variation_num += 1
                template_idx += 1
                phrase_idx += 1
                
                if variation_num > 100000:
                    print(f"    تحذير: وصلنا للحد الأقصى للتنويعات، بننتقل للمرحلة التالية")
                    break
            
            # Phase 2: Compound variations (25% of examples)
            print(f"  المرحلة 2: تنويعات مركبة...")
            compound_target = int(examples_per_char * 0.25)
            
            other_chars = [c for c in ALL_CHARACTERS if c != character]
            compound_variation_num = 0
            
            while examples_generated < (basic_target + compound_target):
                other_char = random.choice(other_chars)
                other_data = enhanced_patterns[other_char]
                
                text = create_compound_arabic_variation(char_data, other_data, all_sentences, compound_variation_num)
                text_hash = hashlib.md5(text.encode('utf-8')).hexdigest()
                
                if validate_arabic_text(text) and text_hash not in global_text_set and text_hash not in char_text_set:
                    global_text_set.add(text_hash)
                    char_text_set.add(text_hash)
                    
                    writer.writerow([row_id, text, emotion, character, cues, category])
                    row_id += 1
                    examples_generated += 1
                    total_generated += 1
                    
                    if examples_generated % 1000 == 0:
                        print(f"    ولَّدنا {examples_generated - basic_target:,}/{compound_target:,} مثال مركب")
                        f.flush()
                
                compound_variation_num += 1
                
                if compound_variation_num > 50000:
                    print(f"    تحذير: وصلنا للحد الأقصى للتنويعات المركبة")
                    break
            
            # Phase 3: Advanced variations (15%)
            print(f"  المرحلة 3: تنويعات متقدمة...")
            advanced_target = examples_per_char
            advanced_variation_num = 0
            
            while examples_generated < advanced_target:
                text = create_arabic_advanced_variation(character, core_phrases, advanced_variation_num)
                text_hash = hashlib.md5(text.encode('utf-8')).hexdigest()
                
                if validate_arabic_text(text) and text_hash not in global_text_set and text_hash not in char_text_set:
                    global_text_set.add(text_hash)
                    char_text_set.add(text_hash)
                    
                    writer.writerow([row_id, text, emotion, character, cues, category])
                    row_id += 1
                    examples_generated += 1
                    total_generated += 1
                    
                    if examples_generated % 1000 == 0:
                        print(f"    ولَّدنا {examples_generated:,}/{examples_per_char:,} مثال إجمالي")
                        f.flush()
                
                advanced_variation_num += 1
                
                if advanced_variation_num > 30000 and examples_generated < advanced_target:
                    print(f"    بنعمل تنويعات بسيطة عشان نوصل للهدف...")
                    while examples_generated < advanced_target:
                        template = random.choice(templates)
                        phrase = random.choice(core_phrases)
                        text = create_realistic_arabic_variation(template, phrase, all_sentences, advanced_variation_num)
                        text_hash = hashlib.md5(text.encode('utf-8')).hexdigest()
                        
                        if validate_arabic_text(text) and text_hash not in global_text_set:
                            global_text_set.add(text_hash)
                            char_text_set.add(text_hash)
                            
                            writer.writerow([row_id, text, emotion, character, cues, category])
                            row_id += 1
                            examples_generated += 1
                            total_generated += 1
                            advanced_variation_num += 1
                    
                    break
            
            char_time = time.time() - char_start_time
            print(f"  ✅ {character}: {examples_generated:,} مثال في {char_time:.1f} ثانية")
    
    total_time = time.time() - start_time
    
    print("\n" + "=" * 70)
    print("✅ اكتمل إنشاء مجموعة البيانات!")
    print("=" * 70)
    print(f"📊 إحصاءات:")
    print(f"   إجمالي الأمثلة المولدة: {total_generated:,}")
    print(f"   أمثلة لكل شخصية: ~{total_generated // len(ALL_CHARACTERS):,}")
    print(f"   وقت التوليد الإجمالي: {total_time:.1f} ثانية ({total_time/60:.1f} دقيقة)")
    print(f"   متوسط السرعة: {total_generated/total_time:.0f} مثال/ثانية")
    print(f"\n📁 ملف الإخراج: {output_path}")
    
    # Verify counts and duplicates
    print("\n🔍 مراجعة الجودة:")
    with codecs.open(output_path, 'r', encoding='utf-8-sig') as f:
        lines = f.readlines()
        line_count = len(lines) - 1
        
        texts = set()
        duplicate_count = 0
        arabic_validation_errors = 0
        reader = csv.reader(lines[1:])
        for row in reader:
            if len(row) > 1:
                text = row[1]
                if text in texts:
                    duplicate_count += 1
                if not validate_arabic_text(text):
                    arabic_validation_errors += 1
                texts.add(text)
    
    print(f"   عدد الأسطر في CSV: {line_count:,}")
    print(f"   النصوص الفريدة: {len(texts):,}")
    print(f"   تكرارات: {duplicate_count}")
    print(f"   أخطاء في التحقق من العربي: {arabic_validation_errors}")
    
    if line_count == total_generated and duplicate_count == 0 and arabic_validation_errors == 0:
        print("   ✓ التحقق من العد ناجح - لا توجد تكرارات أو أخطاء")
    else:
        print(f"   ⚠️ مشاكل: متوقع {total_generated:,}, حصلنا {line_count:,}, تكرارات: {duplicate_count}, أخطاء عربي: {arabic_validation_errors}")
    
    # Generate mapping files
    generate_mapping_files(out_dir)
    
    # Create a sample preview
    create_sample_preview(output_path, out_dir)
    
    return output_path

def generate_mapping_files(out_dir: str):
    """Generate mapping files for perfect dataset reference."""
    mapping_dir = os.path.join(out_dir, "mappings")
    os.makedirs(mapping_dir, exist_ok=True)
    
    # Character mapping
    char_mapping_path = os.path.join(mapping_dir, "character_mapping.json")
    with open(char_mapping_path, 'w', encoding='utf-8-sig') as f:
        json.dump(CHARACTER_MAPPING, f, ensure_ascii=False, indent=2)
    
    # Emotion mapping
    emotion_mapping_path = os.path.join(mapping_dir, "emotion_mapping.json")
    with open(emotion_mapping_path, 'w', encoding='utf-8-sig') as f:
        json.dump(EMOTION_MAPPING, f, ensure_ascii=False, indent=2)
    
    # Category mapping
    category_mapping_path = os.path.join(mapping_dir, "category_mapping.json")
    with open(category_mapping_path, 'w', encoding='utf-8-sig') as f:
        json.dump(CATEGORY_MAPPING, f, ensure_ascii=False, indent=2)
    
    # Character details
    char_details_path = os.path.join(mapping_dir, "character_details.json")
    char_details = {}
    for char_ar in ALL_CHARACTERS:
        char_en = [k for k, v in CHARACTER_MAPPING.items() if v == char_ar][0]
        char_details[char_ar] = {
            "english_name": char_en,
            "emotion": CHARACTER_PATTERNS[char_ar]["emotion"],
            "category": CHARACTER_PATTERNS[char_ar]["category"],
            "description": f"الجزء الذي يمثل {char_ar} في نموذج نظام العائلة الداخلية",
            "core_phrases_count": len(CHARACTER_PATTERNS[char_ar]["core_phrases"]),
            "templates_count": len(CHARACTER_PATTERNS[char_ar]["templates"])
        }
    
    with open(char_details_path, 'w', encoding='utf-8-sig') as f:
        json.dump(char_details, f, ensure_ascii=False, indent=2)
    
    print(f"\n📄 ملفات التعيين:")
    print(f"   ✓ character_mapping.json - {len(CHARACTER_MAPPING)} تعيين")
    print(f"   ✓ emotion_mapping.json - {len(EMOTION_MAPPING)} عاطفة")
    print(f"   ✓ category_mapping.json - {len(CATEGORY_MAPPING)} فئة")
    print(f"   ✓ character_details.json - تفاصيل {len(char_details)} شخصية")

def create_sample_preview(csv_path: str, out_dir: str, sample_size: int = 50):
    """Create a sample preview of the dataset."""
    preview_path = os.path.join(out_dir, "dataset_preview.txt")
    
    with codecs.open(csv_path, 'r', encoding='utf-8-sig') as f:
        reader = csv.reader(f)
        headers = next(reader)
        samples = []
        
        for i, row in enumerate(reader):
            if i < sample_size:
                samples.append(row)
            else:
                break
    
    with codecs.open(preview_path, 'w', encoding='utf-8-sig') as f:
        f.write("=" * 70 + "\n")
        f.write("عينة من مجموعة بيانات IFS بالعربي\n")
        f.write("=" * 70 + "\n\n")
        
        for i, row in enumerate(samples, 1):
            f.write(f"مثال #{i}:\n")
            f.write(f"  النص: {row[1]}\n")
            f.write(f"  العاطفة: {row[2]}\n")
            f.write(f"  الشخصية: {row[3]}\n")
            f.write(f"  الفئة: {row[5]}\n")
            f.write("-" * 50 + "\n")
    
    print(f"   ✓ dataset_preview.txt - عينة من {len(samples)} مثال")

# ---------------- Test Function ----------------
def test_arabic_encoding():
    """Test Arabic encoding to ensure it's working correctly."""
    test_text = "الحقيقة إنّ بحس إني مش كفاية كويس."
    
    print("🔍 اختبار ترميز النص العربي:")
    print(f"   النص الأصلي: {test_text}")
    print(f"   الطول: {len(test_text)} حرف")
    print(f"   الترميز UTF-8: {test_text.encode('utf-8')}")
    print(f"   تصحيح الترميز: {ensure_arabic_encoding(test_text)}")
    print(f"   تحقق من العربي: {validate_arabic_text(test_text)}")
    
    # Test garbled text
    garbled = "Ø§Ù„Ø­Ù‚ÙŠÙ‚Ø© Ø¥Ù†Ù‘ Ø¨Ø­Ø³ Ø¥Ù†ÙŠ Ù…Ø´ ÙƒÙØ§ÙŠØ© ÙƒÙˆÙŠØ³."
    print(f"\n   اختبار نص مشوه: {garbled}")
    fixed = ensure_arabic_encoding(garbled)
    print(f"   بعد التصحيح: {fixed}")
    print(f"   هل هو عربي صحيح؟: {validate_arabic_text(fixed)}")

# ---------------- Main ----------------
def main():
    parser = argparse.ArgumentParser(description="Generate High-Quality Arabic IFS Character Dataset")
    parser.add_argument("--raw_dir", type=str, default=RAW_DATA_DIR, help="Raw data directory")
    parser.add_argument("--out_dir", type=str, default=OUT_DIR_DEFAULT, help="Output directory")
    parser.add_argument("--examples_per_char", type=int, default=EXAMPLES_PER_CHARACTER, 
                       help=f"Examples per character (default: {EXAMPLES_PER_CHARACTER:,})")
    parser.add_argument("--seed", type=int, default=SEED, help="Random seed")
    parser.add_argument("--test", action="store_true", help="Test Arabic encoding only")
    
    args = parser.parse_args()
    
    # Set seed
    random.seed(args.seed)
    
    if args.test:
        test_arabic_encoding()
        return
    
    print("🎯 مولد مجموعة بيانات شخصيات IFS بالعربي عالي الجودة")
    print("=" * 70)
    print(f"هيعمل معالجة للبيانات الخام من: {args.raw_dir}")
    print(f"هيولِّد {args.examples_per_char * len(ALL_CHARACTERS):,} مثال")
    print(f"({args.examples_per_char:,} لكل شخصية من {len(ALL_CHARACTERS)} شخصية)")
    print("من غير تكرارات وواقعية معززة من البيانات الخام.")
    print("=" * 70)
    
    # Check for raw data
    if not os.path.exists(args.raw_dir):
        print(f"\n⚠️ تحذير: مجلد البيانات الخام '{args.raw_dir}' مش موجود.")
        print("   النظام هيصنع بيانات اصطناعية باستخدام الأنماط الأساسية.")
        print("   لأفضل نتيجة، ضيف ملفات نصية للمجلد ده.")
        print("   الصيغ المدعومة: .txt, .csv, .json, .jsonl")
        response = input("\nتكمل بالبيانات الاصطناعية؟ (y/n): ")
        if response.lower() != 'y':
            print("بنخرج...")
            return
    
    # Generate dataset
    dataset_path = generate_high_quality_arabic_dataset(args.raw_dir, args.out_dir, args.examples_per_char)
    
    print("\n" + "=" * 70)
    print("🚀 مجموعة البيانات جاهزة للتدريب!")
    print("=" * 70)
    
    # Calculate file size
    try:
        file_size = os.path.getsize(dataset_path) / 1024 / 1024
        with codecs.open(dataset_path, 'r', encoding='utf-8-sig') as f:
            line_count = sum(1 for _ in f) - 1
        
        print(f"\n📁 مجموعة البيانات اتخزنت في: {dataset_path}")
        print(f"   حجم الملف: ~{file_size:.1f} ميجابايت")
        print(f"   عدد الأمثلة: {line_count:,}")
        print(f"   تقريباً {file_size * 1024 / line_count:.1f} كيلوبايت لكل مثال")
    except:
        print(f"\n📁 مجموعة البيانات اتخزنت في: {dataset_path}")
    
    print("\n💡 نصائح للتدريب:")
    print("1. استخدم مع المحولات (AraBERT/CAMeLBERT) لأفضل نتيجة")
    print("2. عمود 'character_cues' ممكن يحسن فهم النموذج")
    print("3. فكِّر في التعلم متعدد المهام مع كشف المشاعر")
    print("4. مع 360,000 مثال، درِّب لمدة 3-5 عصور")
    print("5. استخدم معدل تعلم 2e-5 مع تسخين خطي")
    print("\n🔧 ميزات مجموعة البيانات:")
    print("   • محسَّن بأنماط من البيانات الخام")
    print("   • تراكيب جمل واقعية بالعربي المصري")
    print("   • لا توجد نصوص مكررة")
    print("   • مطابقة مثالية بين العاطفة والشخصية")
    print("   • نصوص عربية سليمة بدون رموز عشوائية")
    print("   • ملفات تعيين كاملة للرجوع إليها")
    print("   • ترميز UTF-8-BOM متوافق مع إكسل")
    print("\n📖 كيف تشوف البيانات:")
    print(f"   اذا استخدمت إكسل: افتح {dataset_path} مباشرة")
    print(f"   اذا استخدمت Python: pd.read_csv('{dataset_path}', encoding='utf-8-sig')")
    print(f"   اذا استخدمت Excel على Mac: افتح الملف واختار ترميز UTF-8")

if __name__ == "__main__":
    main()